# nanoDD

This repo provides a simple single-file discrete diffusion (D3PM) implementation in PyTorch for research, learning and fun.

Currently the model is trained on text8. The training script is derived from nanoGPT to keep things simple.
